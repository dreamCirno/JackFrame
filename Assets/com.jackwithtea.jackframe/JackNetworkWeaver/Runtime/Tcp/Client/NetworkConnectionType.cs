namespace JackFrame.Network {
    public enum NetworkConnectionType {
        None,
        Connected,
        Connecting,
        ConnectFailed,
        Reconnecting,
        ReconnectFailed,
        Disconnected,
    }

}